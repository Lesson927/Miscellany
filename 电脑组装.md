# 电脑组装（台式）
## CPU
CPU是台式机的大脑  
**几个概念**  
+ IPC（Instructions per clock)（每时钟周期指令数）与频率。IPC是CPU的架构优秀程度的体现，频率和IPC共同决定了CPU的计算性能。越高越好。
+ 访存性能。访存性能和CPU的缓存规模/内存控制器设计有关，主要影响访存密集型应用，比如游戏（对访存延迟有极高的要求）和流体力学模拟（对内存带宽有要求）。  
+ 功耗，代表CPU所需要消耗的能源，越低越好。

**基于以上几个概念， 得出以下几个评价指标：**
+ 单核性能：处理无法并行化处理的逻辑的性能，由单个物理核心的IPC与频率决定，也和访存性能有关。
+ 多核性能：处理大量可并行处理的任务时的性能。对于单核性能相仿的CPU而言，核心数越多，多核性能就越强。也和访存性能有关。
+ 能耗比：对笔记本比较重要，在非ITX的台式机上没那么重要。

**结合不同使用场景，介绍CPU的评价标准：**
+ 对日常使用场景而言，在多核性能满足最低要求的情况下，CPU的单核性能越强，体验就越好。
+ 对大部分单机游戏（尤其是老单机）来说，CPU的性能并不特别重要，这些游戏对计算与访存的要求都不高。
+ 对网络游戏来说，CPU的访存性能和单核性能都很重要。
+ 对模拟类游戏与生产力重负载而言，CPU的多核性能和访存性能很重要。  

## 内存和主板

### 内存
内存是CPU的草稿纸
+ 决定内存性能的三大要素，是容量，频率和时序；内存条的核心要素，是它PCB板上的颗粒。频率决定带宽（Throughput），时序决定绝对延迟（纳秒级）。频率越高，时序越低，那么访存延迟就越低。游戏（尤其是网络游戏）是非常典型的访存密集型任务，选购内存条时，一定要选择频率和时序都合适的内存条。
+ 有的CPU只能使用D4内存，有的CPU只能使用D5内存，有的CPU二者都能用，但不能同时使用D4和D5内存。

### 主板
主板是计算机的基座，它具有如下功能：  
+ 在计算机启动时，进行自检，检查自身和其上各部件
+ 承担各个配件之间的数据传输工作
+ 为其上的各种电子元件和配件供电
+ 基于CPU和芯片组，提供各种拓展通道（PCIe/M2/SATA/USB等)
  
一般来说，选购主板时，需要考虑以下几点：  
+ 兼容性
兼容芯片组，内存插槽，板型
+ CPU供电
主板并不是直接将电源提供的+12V电压直接输给CPU，而需要经过主板上的电路转换，换成CPU所需要的工作电压，才能够输给CPU。我们常说的“主板供电能力”，主要指的就是这个供电电路的能长期稳定承受的负载。
+ 拓展性
 - 主板的USB接口几乎全部由芯片组引出，而台式机主板提供的USB接口数量一般远多于笔记本，不会不够用
 - 主板M2接口的数量，是对普通用户来说，“拓展性”中最重要的要素。它基于CPU和芯片组提供的PCIe通道实现，对普通用户来说，不应少于2个，在接下来的作业栏中，除了最入门的配置，我都会选择至少有2个M2接口的主板。'
 - PCIe插槽，是用于安装PCIe设备的插槽，显卡是最常见的PCIe设备。第一根PCIe插槽的位置很重要，如果你需要安装高端的3槽厚显卡，那么请选择第一根PCIe插槽和主板螺丝孔平齐的主板。
+ 用户友好性
 - BIOS易用性
BIOS是主板上的固件，负责启动整个计算机；各位读者在互联网上看到的“BIOS”往往指代BIOS的界面；主板厂商并不编写全部的BIOS，只能定制BIOS的界面，无法对BIOS本身的逻辑作太多修改。BIOS提供了一些对普通用户来说很重要的BIOS设置项，用户通过BIOS界面来变更这些设置项。
 - Debug灯
Debug灯，是主板自检过程中，用于向外界汇报进度的工具。如果没有debug灯，也没有别的向外汇报进度的工具（例如蜂鸣器），那么，一旦你的台式机出了问题，你将无法简单判断问题的成因。
 - 快拆接口
快拆接口，是一项改善安装体验的用户友好性设计，可有可无，最好有，不值得加钱换快拆。

## 显卡
市场上的显卡制造商有NVIDIA，AMD和Intel三家，实力递减。
### NVIDIA显卡
NVIDIA是这个世界上最强的显卡制造商，其拥有极强的设计能力，产品稳定性高，能耗比高，绝对性能突出。  
显卡型号好比工厂里的工人。显卡型号越先进，工人的能力就越强，生产效率就越高。  
显存好比原料仓库，显存越大仓库越大，能在同一时间内进行的工序就越多。  
#### 缺点
+ 对机械工程类生产力软件的支持很差
+ 定价非常傲慢
+ 对Linux发行版的支持不佳
+ 吝惜显存
#### 优点
+ 对各种特性支持最全面，光追/超分/AI性能强悍。
+ 使用人数极多，各种各样的问题都容易找到解决方案
+ 在很长一段时间内，软件稳定性优于AMD与Intel
### AMD显卡
AMD是唯一能在中高端领域和NVIDIA角力的老牌显卡制造商，但在近十年的竞争中逐渐落后于NVIDIA，目前已暂时放弃推出旗舰产品与NV竞争。
#### 缺点
+ 驱动程序稳定性堪忧
+ 5000系A卡的首发驱动并不完全稳定，留下了掉驱动的恶名
+ 9000系之前的A卡光追基本不可用，9000系仍缺少路径追踪能力
+ 9000系之前的A卡超分基本不可用，9000系的FSR4目前仍需更多游戏适配
#### 优点
+ 对机械工程类生产力软件的适配很好
+ 对同价格的N卡光栅性能极具竞争力，9000系A卡的光追性能和超分技术也赶上了40系N卡的水平
+ 显存容量给得很慷慨，9000系A卡除外

### Intel显卡
#### 缺点
+ 使用人数极少，在冷门游戏和老游戏中的表现不佳
+ A系列显卡待机功耗偏高
+ 对CPU的性能有要求，如果你想使用I卡，至少需要12400f这样的现代CPU
#### 优点
+ 价格极具竞争力
+ 剪视频能力非常强大
+ 驱动更新非常非常频繁
+ 显存容量给得极度慷慨

## 电源
电源也许是台式机最重要的部件，有言曰”丐啥都不能丐电源“。电源，是将220V市电，转换为台式机内各个配件所需要的不同电压的供电接口的配件，具体实现方案多种多样，并无太高技术含量，只要良心到位，小厂也能做出很不错的电源。

## 固态硬盘
固态硬盘的两大主要组成部分，是主控与颗粒。  
主控是固态硬盘的大脑，颗粒的管理者，负责数据的传输与管理；颗粒则是数据的载体。  
固态硬盘主控的型号，往往和这一固态硬盘的接口协议与传输速度强相关。从接口协议来分，可以分为PCIe4.0固态，PCIe3.0固态，以及SATA固态。前两种固态使用M2这一物理规格的接口，后一种固态使用SATA这一物理规格的接口。

## 散热器和机箱
散热器往往指CPU散热器，有多种分类标准。一般情况下，可按照导热方式分为两大类：用金属块/热管传热给鳍片的散热器，和用水泵驱动冷却液往冷排上传热的散热器，前者被称为风冷，后者被称为水冷。  
散热器和CPU的表面，在宏观层面看，是光滑平整的；然而，在微观层面，它们都并不光滑，无法充分接触，需要一些能确保充分接触的导热性能良好的介质来填充它们表面的间隙——一般来说，人们使用硅脂和硅脂的变种（相变片）来完成填充。  




























